# Example Launch Manifest

Use `fixtures/manifest.json` as the minimum useful shape for a launch planning run. Keep claims factual and include exact verification commands.

```bash
repo-launch-skill plan --manifest fixtures/manifest.json --readme fixtures/README.sample.md --format md
```
